server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/PollitosKFC/EasyJobsMicroservice.git
          searchPaths: config-data
          username: ShoLee01
          password: Maag3.1415
          default-label: "master"

  security:
    user:
      name: root
      password: s3cr3t